package tree;

public class ExpressionTree {
    private BinaryNode root;

    /**
     * Constructor that creates a tree with a given root
     * @param root root for this tree
     */
    public ExpressionTree(BinaryNode root) {
        this.root = root;
    }
}